extends layout
block content
	h1 Dodawanie
	form(method="post" action="/add_dealer")
		label Marka
		br
		input(type="text" name="brand")
		br
		input(type="submit" value="Dodaj")
		if message!=''
			p #{message}